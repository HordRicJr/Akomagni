# Akomagni BMAD kernel

Shipped with Akomagni. Synced into the install tree on `install` / `akomagni update`.
Skills are discovered automatically — users do not need to know this path or run `skill link`.
